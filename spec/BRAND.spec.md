---
authority: claude-proposes
---

# Brand

> **Not written.** This is a placeholder so the router resolves and CI passes. It contains
> no specification.
>
> The content was worked out in planning conversation and never written to any store. It is
> not recoverable from a file. See `state/OPEN.state.md` O-18. Either write it from
> scratch or re-decide it — do not let Claude reconstruct it from memory and present the
> result as a record.

## What belongs here

The three pillars, the voice, and the copy rules. Derived using Mark Shaw's workshop
method — what people actually experience, not what the company says about itself.

## What is on record

Aspiration only so far; the truth comes later from real use. Collectables are classified by
how they were made: hand-made ones are attributed to a named artist, generated ones go to
rarity buckets with the administrator as contributing author. A capture yields a digital
ownership code. See `rules/LANGUAGE.rule.md` for the enforced half.
